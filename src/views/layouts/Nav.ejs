<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <!-- Container wrapper -->
  <div class="container">
    <!-- Navbar brand -->
    <a class="navbar-brand me-2" href="/">
      <i class="fa-solid fa-bag-shopping general-color"></i>
    </a>

    <!-- Toggle button -->
    <button
      class="navbar-toggler"
      type="button"
      data-mdb-toggle="collapse"
      data-mdb-target="#navbarButtonsExample"
      aria-controls="navbarButtonsExample"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <i class="fas fa-bars general-color"></i>
    </button>

    <!-- Collapsible wrapper -->
    <div class="collapse navbar-collapse" id="navbarButtonsExample">
      <!-- Left links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link text-warning" href="/">Online Shopping</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold text-warning" href="/">Home</a>
        </li>
        <% if(admin) { %>
        <li class="nav-item">
          <a class="nav-link fw-bold text-warning" href="/products/create"
            >Create Product</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold text-warning" href="/products/admin/get"
            >My Product</a
          >
        </li>
        <% } %>
      </ul>

      <!-- Left links -->
      <% if(admin) { %>
      <div class="d-flex align-items-center">
        <a href="/profile/<%= admin._id %>">
          <button
            type="button"
            class="btn btn-link px-3 me-2 text-warning"
            data-mdb-toggle="tooltip"
            data-mdb-html="true"
            title="<em>Hi</em> <u><%= admin.name %></u>"
          >
            <%= admin.name %>
          </button>
        </a>
        <form action="/logout">
          <button
            type="submit"
            class="btn me-3 text-white bg-warning"
          >
            logOut
          </button>
        </form>
      </div>
      <% } else { %>
      <!-- Left links -->
      <div class="d-flex align-items-center">
        <a href="/login"
          ><button type="button" class="btn btn-link text-warning px-3 me-2">
            Login
          </button></a
        >
      </div>
      <% } %>
    </div>
    <!-- Collapsible wrapper -->
  </div>
  <!-- Container wrapper -->
</nav>
<!-- Navbar -->
